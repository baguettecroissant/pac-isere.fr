---
interface Props {
  cityName: string;
  zip: string;
  departmentName: string;
}

const { cityName, zip, departmentName } = Astro.props;
const query = encodeURIComponent(`${cityName}, ${zip}, ${departmentName}, France`);
---

<div class="map-container" style="width: 100%; height: 320px; border-radius: var(--radius-md); overflow: hidden; border: 1px solid var(--border-color); background: #f1f5f9;">
  <iframe
    title={`Carte de ${cityName}`}
    src={`https://www.google.com/maps?q=${query}&output=embed&hl=fr`}
    width="100%"
    height="100%"
    style="border: 0;"
    allowfullscreen=""
    loading="lazy"
    referrerpolicy="no-referrer-when-downgrade"
  />
</div>
